rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Bloqueia tudo por padrão — acesso apenas via backend (Admin SDK)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
